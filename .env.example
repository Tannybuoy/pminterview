# ============================================
# SECURITY NOTICE
# ============================================
# - NEVER commit .env files to git
# - NEVER use VITE_ prefix for sensitive keys (they get exposed to browser)
# - API keys below are ONLY used in build scripts (Node.js server-side)
# ============================================

# Google Sheets Configuration (used by build scripts only - NOT exposed to browser)
GOOGLE_SHEETS_ID=your_sheet_id_here
GOOGLE_API_KEY=your_api_key_here

# ============================================
# Client-safe config (VITE_ prefix = exposed to browser)
# Only put NON-SENSITIVE values here
# ============================================
VITE_APP_NAME=AI PM Interview Prep
